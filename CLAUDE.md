## Language

日本語で返信してください。

## Package Manager

Use `pnpm`. Not `npm`, `yarn` and `bun`.

## Specification

仕様は `docs/spec.md` にまとまっています。この仕様に無いことや、仕様が不明な点がある場合は `codex` skill を利用して多角的な視点でレビューし、実行に移してください。
